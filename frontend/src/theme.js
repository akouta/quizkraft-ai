import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1976d2", // Your app's primary color
    },
    secondary: {
      main: "#dc004e", // Your app's secondary color
    },
  },
});

export default theme;
